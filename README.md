# Deskhub

## Run

```bash
npm install
npm run dev
```

- Site: **http://localhost:3055/** (open `index.html` — use link or `/` if `serve` lists it)
- API: **http://localhost:3040**

Demo login:

- **Email:** `priya@deskhub.in`
- **Password:** `demo123`

Then open **Dashboard** and **Tickets**. Keep `npm run dev` running.

## GitHub

Do not commit `node_modules`. Use `git push`, not bulk file upload.
